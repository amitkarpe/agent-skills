# Offline Test Assets

These files are fallback assets for regression tests only. Do not use them as the default source for production AWS diagrams. Prepare the latest official AWS icon package with `scripts/prepare_aws_icons.py`.
